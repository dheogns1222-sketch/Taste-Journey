# scripts/archive — 실행 금지 (기록용)

2026-06 개발 당시의 일회성 커밋/배포 스크립트. 옛 브랜치(feature/app-core)·옛 경로·`src/`(폐기)·`.git` 삭제(setup-git) 같은 파괴적 동작이 들어 있어 **우클릭 실행이 안 되도록 `.ps1.txt`로 봉인**했다(2026-08-18). 현행 스크립트는 `scripts/deploy/[배포]-*.ps1`·`scripts/dev/` 뿐.
